name: example
description: A sample command-line application.
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.8.1

dependencies:
  spring_dart:

dev_dependencies:
  build_runner: ^2.6.1
  spring_dart_gen:
  lints: ^5.0.0
  test: ^1.24.0
