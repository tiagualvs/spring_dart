name: spring_dart_core
description: A starting point for Dart libraries or applications.
version: 1.0.0

resolution: workspace

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
