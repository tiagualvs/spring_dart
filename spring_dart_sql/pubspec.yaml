name: spring_dart_sql
description: A starting point for Dart libraries or applications.
version: 1.0.0

publish_to: none

environment:
  sdk: ^3.8.1

dependencies:
  spring_dart_core:
    git:
      url: https://github.com/tiagualvs/spring_dart.git
      path: spring_dart_core
      ref: dev

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
